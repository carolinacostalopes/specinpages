openapi: 3.0.0
info:
  version: 1.0.0

paths:
  "/example"
    parameters:
      - $ref: "./domainspec.yml#/components/parameters/id"
    get:
      summary: Retrieves examples
    responses:
      "200": 
        $ref: "./otherdomainspec.yml#/responses/success"
